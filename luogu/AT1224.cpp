#include <iostream>
using namespace std;

int n, sum[200010];
string str;

int main() {
	cin >> n;
	cin >> str;
	for (int i=0; i<n; i++) {
		if (str[i] == 'J')
		if (str[i] == 'O')
		if (str[i] == 'I')
	}
}